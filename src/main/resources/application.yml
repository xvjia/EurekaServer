server:
  port: 8761
eureka:
  instance:
    hostname: http://www.xvjialing.ink
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/